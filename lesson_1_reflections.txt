How did viewing a diff between two versions of a file help you see the bug that
was introduced?

    Por que se puede observar de manera clara y sencilla los cambios recientes que se hicieron el proyecto

How could having easy access to the entire history of a file make you a more
efficient programmer in the long term?

    Por que es muy sencillo ver los cambios que se han realizado a un documento y poder extraer información de ellos o deshacer algunos cambios realizados

What do you think are the pros and cons of manually choosing when to create a
commit, like you do in Git, vs having versions automatically saved, like Google
docs does?

    La ventaja es que solo guardaremos cambios significativos y de esa manera el historial de versiones será más consiso hacerca de los cambios realizados

Why do you think some version control systems, like Git, allow saving multiple
files in one commit, while others, like Google Docs, treat each file separately?

    Por que en programación es muy comun que los archivos esten relacionados entre si, y que los cambios en un archivo tengan impacto en otros, por eso
    es mejor que se guarden el estado de los archivos en un determinado momentos y no cada uno por separado.

How can you use the commands git log and git diff to view the history of files?

    Abriendo la consola en la carpeta del repositorio correspondiente se pueden ejecutar los comandos de git "log" y "diff" para ver el historial de cambios realizados

How might using version control make you more confident to make changes that
could break something?

    Por que siempre podemos regresar a un punto de restauración con un simple comando... entonces podemos hacer cambios drasticos sin preocuparnos por hechar todo a perder

Now that you have your workspace set up, what do you want to try using Git for?

    Fill in your answer here